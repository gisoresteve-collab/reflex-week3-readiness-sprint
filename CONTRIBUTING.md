# Contribution Rules
Do not push directly to main.
Each task must have a GitHub Issue.
Each task should use its own branch.
Changes must be submitted through a Pull Request.
At least one teammate reviews each Pull Request.
The Team Leader performs the final merge.
Do not add new features after the Day 1 freeze.
Documentation must reflect the actual Reflex system.
Do not claim functionality that has not been implemented or tested.
